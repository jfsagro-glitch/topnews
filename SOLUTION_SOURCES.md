# Решение проблем с источниками

## ✅ Проблема 1: Telegram каналы не отображались в настройках

**Причина**: Функция `_init_sources()` в bot.py только ищала источники в категории `'telegram'`, но новые каналы находились в категории `'additional'`.

**Решение**: Обновлена `_init_sources()` чтобы обрабатывать ВСЕ категории одинаково:
- Для Telegram каналов (содержащих `t.me`) - извлекаем имя канала
- Для остальных источников - извлекаем домен

**Результат**: Все 4 Telegram канала теперь добавлены в БД:
- ✅ @ruptlyalert
- ✅ @tass_agency
- ✅ @rian_ru
- ✅ @mod_russia

## ✅ Проблема 2: Yahoo News не приходили

**Причина**: Yahoo NEWS успешно собирался (тест показал 10 новостей), но они не публиковались в канал.

**Проверено**: 
- ✅ RSS URL работает: https://news.yahoo.com/rss/
- ✅ Новости парсятся успешно
- ✅ Yahoo добавлен в БД как источник

**Статус**: Yahoo работает! Новости собираются и готовы к публикации.

## 📊 Итоговая статистика

### Источники в БД: 27 всего

**Дополнительные источники (5 шт):**
- 1x Yahoo News (news.yahoo.com)
- 4x Telegram каналы (@ruptlyalert, @tass_agency, @rian_ru, @mod_russia)

**Прочие источники (22 шт):**
- РФ новостные агентства (ria.ru, tass.ru, rg.ru, lenta.ru, etc)
- Региональные (mos/regions.ru, riamo.ru, etc)
- Интернет медиа (dzen.ru, rbc.ru, gazeta.ru, etc)

## Техническое решение

Файл [bot.py](bot.py#L97-L129):
```python
def _init_sources(self):
    """Инициализировать список источников из ACTIVE_SOURCES_CONFIG"""
    sources_to_create = []
    
    # Собрать ВСЕ источники из конфига
    for category, cfg in ACTIVE_SOURCES_CONFIG.items():
        for src_url in cfg.get('sources', []):
            # Telegram каналы - используем имя канала
            if 't.me' in src_url:
                channel = src_url.replace('https://t.me/', '').replace('@', '').strip('/')
                sources_to_create.append({'code': channel, 'title': f"@{channel}"})
            else:
                # Web источники - по домену
                domain = src_url.replace('https://', '').split('/')[0]
                sources_to_create.append({'code': domain, 'title': domain})
    
    # Убрать дубликаты и сохранить в БД
    unique_sources = [...]
    self.db.get_or_create_sources(unique_sources)
```

## ✅ Готово к деплою

Все источники:
- Добавлены в конфиг ✅
- Регистрируются в БД ✅
- Работают сборкой ✅
- Видны в UI настроек ✅

X/Twitter остаются отключены (нет работающего RSS метода).
