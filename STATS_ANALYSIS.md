# Анализ статистики использования DeepSeek API

## Данные из DeepSeek Dashboard (скриншот)

**Период:** Февраль 2026  
**Дата проверки:** 1 февраля 2026

### Фактическое использование API:
- **API запросов:** 626
- **Токенов использовано:** 197,947  
- **Стоимость за месяц:** $0.02 USD
- **Баланс:** $3.34 USD

---

## Расчет по данным

### Средняя стоимость за токен:
$0.02 / 197,947 токенов = **$0.000000101 за токен** ($0.101 за 1M токенов)

### Средние токены на запрос:
197,947 / 626 = **316 токенов на запрос**

### Ожидаемая структура использования:

Система отслеживает 3 типа AI операций:

1. **📝 Пересказы (Summarize)** - по запросу пользователя
   - Используется больше всего токенов (до 500 max_tokens на ответ)
   - Обрабатывает полный текст статьи
   - Температура: 0.7 (более креативный текст)

2. **🏷️ Верификация категорий (Category)** - автоматически, 30% статей
   - Минимальное использование токенов (max_tokens=20)
   - Только заголовок + краткий текст (до 1000 символов)
   - Температура: 0.3 (детерминированная классификация)

3. **✨ Очистка текста (Text Clean)** - автоматически, 30% статей  
   - Средний расход токенов (max_tokens=500)
   - Убирает навигацию и мусор из текста
   - Температура: 0.2 (точное извлечение)

---

## Проблемы обнаружены и исправлены

### 🐛 Проблема #1: Пересказ не работал

**Причина:** В методе `summarize()` возвращался неправильный формат:
```python
# БЫЛО (неправильно):
return None, 0

# СТАЛО (правильно):
return None, {"input_tokens": 0, "output_tokens": 0, "total_tokens": 0}
```

**Исправлено в коммите:** `a8ea36e`

**Эффект:** Теперь кнопка "ИИ" будет корректно работать и отслеживать токены.

---

### 🐛 Проблема #2: Статистика не собиралась по типам операций

**Причина:** Методы `verify_category()` и `extract_clean_text()` не возвращали информацию о токенах.

**Исправлено в коммите:** `207fc01`

**Изменения:**
- ✅ `verify_category()` теперь возвращает `(category, token_usage)`
- ✅ `extract_clean_text()` теперь возвращает `(clean_text, token_usage)`
- ✅ SourceCollector логирует токены для category и text_clean
- ✅ Bot логирует токены для summarize
- ✅ База данных отслеживает каждый тип отдельно

---

## Текущая архитектура отслеживания

### База данных (SQLite):
```sql
CREATE TABLE ai_usage (
    id INTEGER PRIMARY KEY CHECK (id = 1),  -- Singleton pattern
    total_requests INTEGER,
    total_tokens INTEGER,
    total_cost_usd REAL,
    summarize_requests INTEGER,
    summarize_tokens INTEGER,
    category_requests INTEGER,
    category_tokens INTEGER,
    text_clean_requests INTEGER,
    text_clean_tokens INTEGER,
    updated_at TIMESTAMP
)
```

### Ценообразование DeepSeek:
- **Input tokens:** $0.14 / 1M токенов
- **Output tokens:** $0.28 / 1M токенов

### Расчет стоимости в коде:
```python
input_cost = (token_usage['input_tokens'] / 1_000_000.0) * 0.14
output_cost = (token_usage['output_tokens'] / 1_000_000.0) * 0.28
cost_usd = input_cost + output_cost
```

---

## Проверка статистики

### Как проверить данные в боте:

1. Отправьте команду `/status` в Telegram бот
2. Увидите детальную статистику:
   ```
   🧠 ИИ использование:
   Всего запросов: XXX
   Всего токенов: XXX,XXX
   Стоимость: $X.XXXX
   
   📝 Пересказы: X запросов, X токенов
   🏷️ Категории: X запросов, X токенов
   ✨ Очистка текста: X запросов, X токенов
   ```

### Как данные сохраняются:

✅ **Накопительно** - счетчики только увеличиваются  
✅ **Персистентно** - SQLite файл сохраняется на Railway volume  
✅ **По всем пользователям** - глобальная статистика (id=1)  
✅ **Не сбрасывается** - данные переживают деплои и рестарты

---

## Ожидаемое распределение токенов

После того как бот поработает некоторое время, ожидаю увидеть:

- **Пересказы:** ~70-80% от общего числа токенов (самые "дорогие" запросы)
- **Очистка текста:** ~15-20% (средний расход)
- **Категории:** ~5-10% (минимальный расход)

Это нормально, т.к. пользовательские запросы на пересказ используют больше токенов (полный текст + большой ответ).

---

## Следующие шаги

1. ✅ Дождаться деплоя исправлений на Railway
2. ✅ Проверить `/status` в боте через 5-10 минут
3. ✅ Протестировать кнопку "ИИ" на любой новости
4. ✅ Сверить данные `/status` с DeepSeek dashboard через сутки

---

**Дата анализа:** 1 февраля 2026  
**Статус:** Исправления задеплоены, ожидаем перезапуск на Railway
