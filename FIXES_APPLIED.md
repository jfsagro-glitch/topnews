# ✅ Исправления DeepSeek API и фильтрации мусора

## Что было исправлено

### 1. ❌ → ✅ DeepSeek API endpoint
**Проблема:** API ключ был добавлен, но API возвращал 400 Bad Request

**Причина:** Неправильный endpoint

**Решение:**
```python
# БЫЛО (неправильно):
DEEPSEEK_API_ENDPOINT = 'https://api.deepseek.com/v1/chat/completions'

# СТАЛО (правильно):
DEEPSEEK_API_ENDPOINT = 'https://api.deepseek.com/chat/completions'
```

### 2. ❌ → ✅ Мусор в новостях
**Проблема:** В конце новостей появлялся мусор:
```
☺ Выиграть айфоны — здесь Прислать новость
```

**Причина:** Telegram каналы добавляют призывы к конкурсам/подписке

**Решение:** Добавлены фильтры в `text_cleaner.py`:
```python
# Фильтры для Telegram мусора
r'[\u263a\ud83d\ude0a]+\\s*Выиграть.*[—-]\\s*здесь',
r'Прислать новость',
r'Выиграть\\s+айфон[ыа]?',
r'Конкурс\\s*[—-]\\s*здесь',
r'Написать\\s+нам',
r'Подписаться\\s+на\\s+канал',
```

## Проверка работоспособности

### После деплоя на Railway (1-2 минуты):

1. **Проверить AI пересказ:**
   - Открой Telegram бот
   - Нажми на кнопку "🤖 ИИ" под любой новостью
   - Должен появиться пересказ (не "ИИ не доступен")

2. **Проверить фильтрацию мусора:**
   - Посмотри новости из каналов shot_shot, bazabazon, mash, mod_russia
   - В конце НЕ должно быть:
     - Смайликов ☺ 🎉 🎁
     - "Выиграть айфоны"
     - "Прислать новость"
     - "Конкурс — здесь"

### Что должно работать:
✅ AI пересказ новостей  
✅ Чистый текст без конкурсов/спама  
✅ Корректные Telegram новости  

### Если всё ещё не работает AI:

1. Проверь Railway Variables:
   ```
   DEEPSEEK_API_KEY = sk-xxxxxxxxxxxxxxxxxx (48 символов)
   ```

2. Проверь Railway Logs:
   - Не должно быть `status=400`
   - Должно быть `status=200` для DeepSeek

3. Проверь баланс на https://platform.deepseek.com/account/balance

## Технические детали

**Файлы изменены:**
- `config/config.py` - исправлен endpoint
- `utils/text_cleaner.py` - добавлены фильтры мусора

**Commit:** `078ce7c`

**Deploy:** Автоматически на Railway после push
