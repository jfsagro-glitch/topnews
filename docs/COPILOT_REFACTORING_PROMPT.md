# PROMPT ДЛЯ GITHUB COPILOT / CURSOR

## Проект: Telegram-бот для агрегации новостей

**Цель:** исправить текущие проблемы, повысить стабильность, привести код и архитектуру в корректное и юридически безопасное состояние

---

## 🧠 Роль

Ты — senior backend-разработчик и архитектор, специализируешься на:

- асинхронных Python-приложениях
- Telegram Bot API
- RSS / HTML агрегации новостей
- устойчивых системах с частыми опросами источников

Твоя задача — проанализировать существующий код проекта и внести точечные исправления и улучшения, не переписывая проект с нуля.

---

## 1. Функциональные требования (уже согласованы)

### 1.1 Общая логика

- Бот собирает новости каждые 2 минуты
- Работает 24/7
- Есть ручной запуск сбора по команде /sync
- Публикуются все новости, без фильтрации
- Повторы допускаются, если это новая публикация или развитие темы
- Новости публикуются в порядке поступления

### 1.2 Формат публикации

Каждая новость публикуется отдельным сообщением:

```
Заголовок

Первый абзац текста

Источник: <название источника>
<ссылка>

#Категория
```

Ограничения:

- только заголовок + 1 абзац
- без изображений
- текст очищается от рекламы и HTML-мусора
- жёсткий лимит длины:
  - публикация: до ~1200–1500 символов
  - COPY: до ~2500–3000 символов

### 1.3 Категории

Используются строго 3 категории:

- #Мир
- #Россия
- #Подмосковье

Правила:

- одна новость = один основной хештег
- категория задаётся по источнику, не по тексту

### 1.4 Команды бота

- `/start` — приветствие
- `/help` — помощь
- `/sync` — принудительный сбор
- `/status` — статистика БД
- `/pause` — пауза автосбора
- `/resume` — возобновление

Команды не должны попадать в pipeline публикации как новости.

---

## 2. Текущее состояние проекта (важно учитывать)

### 2.1 Источники

Используются реальные RSS URL, не homepage:

```python
rss_sources = {
  'https://ria.ru/export/rss2/archive/index.xml': 'РИА Новости',
  'https://lenta.ru/rss/': 'Лента.ру',
  'https://www.gazeta.ru/rss/': 'Газета.ру',
  'https://tass.ru/rss/index.xml': 'ТАСС',
  # ...
}

html_sources = {
  'https://dzen.ru/news/rubric/chronologic': 'Яндекс.Дзен',
  'https://ren.tv/news': 'РЕН ТВ',
  'https://360.ru/rubriki/mosobl/': '360 (Подмосковье)',
  # ...
}
```

### 2.2 База данных (SQLite)

- Используется одно общее соединение
- Включён WAL и busy_timeout
- Записи сериализуются через threading.Lock
- Проблема `database is locked` устранена, но архитектура может быть улучшена

### 2.3 Source Collector

- Все RSS и HTML источники запускаются асинхронно
- Используется asyncio.gather
- Добавлен collection_lock, чтобы не было параллельных сборов

### 2.4 Уже исправлено

- ❌ Telegram token больше не логируется
- ❌ Дублирующиеся логи убраны
- ✅ Реальные RSS URLs
- ✅ Команды не публикуются как новости
- ✅ Dockerfile содержит ca-certificates
- ✅ SSL ошибки исправлены
- ✅ Добавлен collection_lock

---

## 3. Требуемые доработки (что нужно сделать)

### 3.1 Единый асинхронный HTTP-клиент

Вынести сетевой доступ в единый Async HTTP client

Использовать:

- реалистичный User-Agent
- Accept, Accept-Language
- таймауты
- retries + exponential backoff + jitter

Использовать один клиент для RSS и HTML

### 3.2 Ограничение параллелизма

В SourceCollector добавить `asyncio.Semaphore`

Ограничить одновременные HTTP-запросы (рекомендуемо 4–8)

### 3.3 RSS — Conditional GET

Для RSS:

- хранить ETag и Last-Modified
- использовать If-None-Match и If-Modified-Since
- при 304 Not Modified — не парсить

это снижает нагрузку и риск блокировок

### 3.4 HTML — Best-effort + cooldown

Для HTML-источников:

- если получен 403 или 429:
  - не обходить защиту
  - зафиксировать warning
  - поставить источник на cooldown (например, 10 минут)
  - не использовать headless-браузеры
  - не пытаться обходить JS challenge / Cloudflare

### 3.5 SQLite и async

Текущая схема с threading.Lock рабочая, но:

**либо:**

- вынести запись в БД в `asyncio.to_thread`

**либо (предпочтительно):**

- реализовать одного writer-а через `asyncio.Queue`
- один writer → одно соединение → никаких блокировок event loop

### 3.6 Защита event loop

любые потенциально блокирующие операции:

- SQLite commit
- тяжёлый парсинг

должны не блокировать event loop

---

## 4. Юридические требования (ОБЯЗАТЕЛЬНО)

### 4.1 Общий принцип

Реализация не должна нарушать законодательство РФ, включая:

- УК РФ
- ГК РФ (авторское право, базы данных)
- условия использования сайтов

Бот предназначен для внутреннего использования, не является СМИ.

### 4.2 Запрещено

- ❌ Обход антибот-защиты
- ❌ Cloudflare / JS challenge / CAPTCHA
- ❌ Headless-браузеры для обхода ограничений
- ❌ Неофициальные / утёкшие API
- ❌ Массовый скрейпинг архивов

### 4.3 Разрешено

- ✅ Использование официальных RSS-лент
- ✅ Публичные HTML-страницы без ограничений
- ✅ Telegram-каналы через официальный API
- ✅ Закрытые источники только при официальном доступе заказчика

### 4.4 Авторские права

не копировать статьи целиком

публиковать:

- заголовок
- 1 абзац
- ссылку
- источник

это агрегатор, не републикатор

### 4.5 Политика деградации

Если источник:

- блокирует
- меняет правила
- становится недоступным

→ бот:

- не ломается
- не пытается обходить
- временно отключает источник
- продолжает работу с остальными

---

## 5. Архитектурные требования

Код должен:

- быть читаемым
- иметь явное разделение:
  - network
  - parsers
  - collector
  - db
  - bot
- позволять легко отключать источники
- не содержать «магии» и скрытых обходов

---

## 6. Ожидаемый результат

После доработок:

- стабильный сбор каждые 2 минуты
- отсутствие 403-флуда
- отсутствие блокировок SQLite
- минимальная нагрузка на источники
- юридически безопасная реализация
- готовность к дальнейшему расширению

---

## Задача

Проанализируй текущий код проекта и внеси изменения строго в рамках описанных требований, предлагая минимально инвазивные, но архитектурно корректные решения.
