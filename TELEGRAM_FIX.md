# Исправления для Telegram каналов

## Проблема
Каналы Telegram (mash, bazabazon, shot_shot) отображались как красные (🔴) с количеством 0, хотя RSSHub успешно собирал новости.

## Причина
Команда `/status` показывала количество **опубликованных** новостей из БД вместо **собранных** в последнем цикле. Цвет индикатора зависел только от `source_health`, а не от реального количества собранных новостей.

## Исправления

### bot.py (строки 230-260)
1. **Изменена логика отображения Telegram каналов:**
   - Теперь показывает количество **собранных** новостей (`last_collected_counts`)
   - Индикатор 🟢 (зелёный) когда `collected_count > 0`
   - Индикатор 🔴 (красный) когда `collected_count == 0`

2. **Упрощён формат вывода:**
   ```
   📡 Каналы Telegram:
   🟢 mash: 10
   🟢 bazabazon: 10
   🟢 shot_shot: 10
   ```

### Результат
- ✅ Telegram каналы теперь корректно отображаются с зелёными индикаторами
- ✅ Показывается актуальное количество собранных новостей
- ✅ RSSHub работает корректно и собирает по 10 новостей из каждого канала
- ✅ Логика применена и к остальным источникам (сайтам)

## Проверка
Запустите тесты:
```bash
.\.venv\Scripts\python.exe test_telegram_collection.py
.\.venv\Scripts\python.exe test_status.py
```

Оба теста подтверждают успешный сбор и отображение новостей из Telegram каналов.
